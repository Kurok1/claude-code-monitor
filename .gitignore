bin/
data/
captured/
*.duckdb
*.duckdb.wal
.DS_Store
config.yaml
config.dev.yaml

# playwright-cli browser snapshots/screenshots (debug tooling output)
.playwright-cli/

# Frontend
node_modules/
frontend/dist/

# Embedded SPA assets produced by `npm run build` in frontend/.
# Keep the directory (so //go:embed compiles) but ignore the build output.
internal/web/dist/*
!internal/web/dist/.gitkeep
