-- 11 event tables (5 tier-1 + 6 tier-2). Schema source: docs/models.md §3 and §4.

CREATE TABLE event_user_prompt (
    ts                    TIMESTAMP NOT NULL,
    received_at           TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    event_sequence        BIGINT,
    prompt_id             VARCHAR,
    workspace_host_paths  VARCHAR[],
    session_id            VARCHAR,
    user_id               VARCHAR   NOT NULL,
    user_account_uuid     VARCHAR,
    user_account_id       VARCHAR,
    user_email            VARCHAR,
    organization_id       VARCHAR,
    app_version           VARCHAR,
    terminal_type         VARCHAR,
    prompt_length         INTEGER,
    prompt                VARCHAR,
    command_name          VARCHAR,
    command_source        VARCHAR,
    attrs                 VARCHAR
);

CREATE TABLE event_api_request (
    ts                     TIMESTAMP NOT NULL,
    received_at            TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    event_sequence         BIGINT,
    prompt_id              VARCHAR,
    workspace_host_paths   VARCHAR[],
    session_id             VARCHAR,
    user_id                VARCHAR   NOT NULL,
    user_account_uuid      VARCHAR,
    user_account_id        VARCHAR,
    user_email             VARCHAR,
    organization_id        VARCHAR,
    app_version            VARCHAR,
    terminal_type          VARCHAR,
    model                  VARCHAR,
    cost_usd               DOUBLE,
    duration_ms            BIGINT,
    input_tokens           BIGINT,
    output_tokens          BIGINT,
    cache_read_tokens      BIGINT,
    cache_creation_tokens  BIGINT,
    request_id             VARCHAR,
    speed                  VARCHAR,
    query_source           VARCHAR,
    effort                 VARCHAR,
    attrs                  VARCHAR
);

CREATE TABLE event_api_error (
    ts                    TIMESTAMP NOT NULL,
    received_at           TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    event_sequence        BIGINT,
    prompt_id             VARCHAR,
    workspace_host_paths  VARCHAR[],
    session_id            VARCHAR,
    user_id               VARCHAR   NOT NULL,
    user_account_uuid     VARCHAR,
    user_account_id       VARCHAR,
    user_email            VARCHAR,
    organization_id       VARCHAR,
    app_version           VARCHAR,
    terminal_type         VARCHAR,
    model                 VARCHAR,
    error                 VARCHAR,
    status_code           INTEGER,
    duration_ms           BIGINT,
    attempt               INTEGER,
    request_id            VARCHAR,
    speed                 VARCHAR,
    query_source          VARCHAR,
    effort                VARCHAR,
    attrs                 VARCHAR
);

CREATE TABLE event_tool_result (
    ts                       TIMESTAMP NOT NULL,
    received_at              TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    event_sequence           BIGINT,
    prompt_id                VARCHAR,
    workspace_host_paths     VARCHAR[],
    session_id               VARCHAR,
    user_id                  VARCHAR   NOT NULL,
    user_account_uuid        VARCHAR,
    user_account_id          VARCHAR,
    user_email               VARCHAR,
    organization_id          VARCHAR,
    app_version              VARCHAR,
    terminal_type            VARCHAR,
    tool_name                VARCHAR,
    tool_use_id              VARCHAR,
    success                  BOOLEAN,
    duration_ms              BIGINT,
    error_type               VARCHAR,
    error                    VARCHAR,
    decision_type            VARCHAR,
    decision_source          VARCHAR,
    tool_input_size_bytes    BIGINT,
    tool_result_size_bytes   BIGINT,
    mcp_server_scope         VARCHAR,
    tool_parameters          VARCHAR,
    tool_input               VARCHAR,
    attrs                    VARCHAR
);

CREATE TABLE event_tool_decision (
    ts                    TIMESTAMP NOT NULL,
    received_at           TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    event_sequence        BIGINT,
    prompt_id             VARCHAR,
    workspace_host_paths  VARCHAR[],
    session_id            VARCHAR,
    user_id               VARCHAR   NOT NULL,
    user_account_uuid     VARCHAR,
    user_account_id       VARCHAR,
    user_email            VARCHAR,
    organization_id       VARCHAR,
    app_version           VARCHAR,
    terminal_type         VARCHAR,
    tool_name             VARCHAR,
    tool_use_id           VARCHAR,
    decision              VARCHAR,
    source                VARCHAR,
    attrs                 VARCHAR
);

CREATE TABLE event_api_retries_exhausted (
    ts                       TIMESTAMP NOT NULL,
    received_at              TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    event_sequence           BIGINT,
    prompt_id                VARCHAR,
    workspace_host_paths     VARCHAR[],
    session_id               VARCHAR,
    user_id                  VARCHAR   NOT NULL,
    user_account_uuid        VARCHAR,
    user_account_id          VARCHAR,
    user_email               VARCHAR,
    organization_id          VARCHAR,
    app_version              VARCHAR,
    terminal_type            VARCHAR,
    model                    VARCHAR,
    error                    VARCHAR,
    status_code              INTEGER,
    total_attempts           INTEGER,
    total_retry_duration_ms  BIGINT,
    speed                    VARCHAR,
    attrs                    VARCHAR
);

CREATE TABLE event_compaction (
    ts                    TIMESTAMP NOT NULL,
    received_at           TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    event_sequence        BIGINT,
    prompt_id             VARCHAR,
    workspace_host_paths  VARCHAR[],
    session_id            VARCHAR,
    user_id               VARCHAR   NOT NULL,
    user_account_uuid     VARCHAR,
    user_account_id       VARCHAR,
    user_email            VARCHAR,
    organization_id       VARCHAR,
    app_version           VARCHAR,
    terminal_type         VARCHAR,
    trigger               VARCHAR,
    success               BOOLEAN,
    duration_ms           BIGINT,
    pre_tokens            BIGINT,
    attrs                 VARCHAR
);

CREATE TABLE event_permission_mode_changed (
    ts                    TIMESTAMP NOT NULL,
    received_at           TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    event_sequence        BIGINT,
    prompt_id             VARCHAR,
    workspace_host_paths  VARCHAR[],
    session_id            VARCHAR,
    user_id               VARCHAR   NOT NULL,
    user_account_uuid     VARCHAR,
    user_account_id       VARCHAR,
    user_email            VARCHAR,
    organization_id       VARCHAR,
    app_version           VARCHAR,
    terminal_type         VARCHAR,
    from_mode             VARCHAR,
    to_mode               VARCHAR,
    trigger               VARCHAR,
    attrs                 VARCHAR
);

CREATE TABLE event_mcp_server_connection (
    ts                    TIMESTAMP NOT NULL,
    received_at           TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    event_sequence        BIGINT,
    prompt_id             VARCHAR,
    workspace_host_paths  VARCHAR[],
    session_id            VARCHAR,
    user_id               VARCHAR   NOT NULL,
    user_account_uuid     VARCHAR,
    user_account_id       VARCHAR,
    user_email            VARCHAR,
    organization_id       VARCHAR,
    app_version           VARCHAR,
    terminal_type         VARCHAR,
    status                VARCHAR,
    transport_type        VARCHAR,
    server_scope          VARCHAR,
    duration_ms           BIGINT,
    error_code            VARCHAR,
    server_name           VARCHAR,
    error                 VARCHAR,
    attrs                 VARCHAR
);

CREATE TABLE event_skill_activated (
    ts                    TIMESTAMP NOT NULL,
    received_at           TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    event_sequence        BIGINT,
    prompt_id             VARCHAR,
    workspace_host_paths  VARCHAR[],
    session_id            VARCHAR,
    user_id               VARCHAR   NOT NULL,
    user_account_uuid     VARCHAR,
    user_account_id       VARCHAR,
    user_email            VARCHAR,
    organization_id       VARCHAR,
    app_version           VARCHAR,
    terminal_type         VARCHAR,
    skill_name            VARCHAR,
    invocation_trigger    VARCHAR,
    skill_source          VARCHAR,
    plugin_name           VARCHAR,
    marketplace_name      VARCHAR,
    attrs                 VARCHAR
);

CREATE TABLE event_at_mention (
    ts                    TIMESTAMP NOT NULL,
    received_at           TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    event_sequence        BIGINT,
    prompt_id             VARCHAR,
    workspace_host_paths  VARCHAR[],
    session_id            VARCHAR,
    user_id               VARCHAR   NOT NULL,
    user_account_uuid     VARCHAR,
    user_account_id       VARCHAR,
    user_email            VARCHAR,
    organization_id       VARCHAR,
    app_version           VARCHAR,
    terminal_type         VARCHAR,
    mention_type          VARCHAR,
    success               BOOLEAN,
    attrs                 VARCHAR
);
